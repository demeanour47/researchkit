/** The calculator's catalogue id, unchanged from when it was listed as coming soon. */
export const TOOL_ID = "reading-time-calculator";

/** The calculator's address. Provisional until the URL strategy (ADR-0005) is accepted. */
export const TOOL_PATH = "/tools/reading-time";
