/** The generator's catalogue id, unchanged from when it was listed as coming soon. */
export const TOOL_ID = "apa-citation-generator";

/** The generator's address. Provisional until the URL strategy (ADR-0005) is accepted. */
export const TOOL_PATH = "/tools/apa-citation-generator";
