/** The Variables Builder catalogue id. */
export const TOOL_ID = "variables-builder";

/** The Variables Builder address. Provisional until the URL strategy (ADR-0005) is accepted. */
export const TOOL_PATH = "/tools/variables-builder";
