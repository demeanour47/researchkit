/** The word counter's catalogue id. */
export const TOOL_ID = "word-counter";

/** The word counter's address. Provisional until the URL strategy (ADR-0005) is accepted. */
export const TOOL_PATH = "/tools/word-counter";
