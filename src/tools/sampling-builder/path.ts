/** The Sampling Technique Builder catalogue id. */
export const TOOL_ID = "sampling-builder";

/** The Sampling Technique Builder address. Provisional until the URL strategy (ADR-0005) is accepted. */
export const TOOL_PATH = "/tools/sampling-builder";
