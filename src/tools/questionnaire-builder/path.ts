/** The Questionnaire Builder catalogue id. */
export const TOOL_ID = "questionnaire-builder";

/** The Questionnaire Builder address. Provisional until the URL strategy (ADR-0005) is accepted. */
export const TOOL_PATH = "/tools/questionnaire-builder";
